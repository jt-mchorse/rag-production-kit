import type { NextConfig } from "next";

const config: NextConfig = {
  // The demo is intentionally minimal — no images, no edge runtime, no
  // i18n. Defaults are the right shape.
};

export default config;
